import './Hero.css'

function Hero({className="", ...props}){
    return(
        <section className={`web-section hero-section radial-bg ${className}`}>
        
        </section>
    )
}

export default Hero;